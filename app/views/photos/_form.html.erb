<%= form_for :photo, :url => "/photos/#{@photo.id}", :method => 'patch' do |f| %>
  <%= f.hidden_field :album_id %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <%= f.submit "Update", :class => 'btn btn-type-primary' %>
<% end %>  